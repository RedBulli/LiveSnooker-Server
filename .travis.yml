language: node_js
sudo: false
node_js:
  - '0.10.29'

addons:
  postgresql: "9.3"

before_install:
  - npm config set ca ""

before_script:
  - psql -c 'create database livesnooker_test;' -U postgres

services:
  - redis-server
  - mongodb
